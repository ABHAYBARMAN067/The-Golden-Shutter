# TODO - Fix 401 Unauthorized on AdminDashboard /api/weddings

- [ ] Reproduce root cause of 401: mismatch between adminToken expected format and token being sent from frontend.
- [ ] Update backend middleware `authMiddleware.js` (or frontend token handling) so that token generated by `/api/auth/login` is validated correctly.
- [ ] Add a temporary debug response/logging to confirm decoded credentials match.
- [ ] Test login -> fetch `/api/weddings` works in browser.
- [ ] Remove debug if added.

